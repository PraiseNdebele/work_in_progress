<h1>Users</h1>

<table class = "table">
<tbody>
	<% @users.each do |user| %>
	<tr>
		<td><%= user.name %> </td>
		<td class= "link"><%= link_to 'Show', user_path(user) %></td>
	</tr>
	<% end %>
</tbody>
</table>

</br>

<%= link_to 'New user', new_user_path %> | <%= link_to 'Home', root_path %>